defaults:
  - io: default
  - db: default
  - globus: default
  - _self_                     # Ensures overriding behavior

project:
  name: demo
  subname: big-soy
run:
  stage: infer
  dataset: semifield_v1

seed: 42

query:
runtime:
paths:

job:
  now: ${now:%Y-%m-%d_%H-%M-%S}
  
run_id: ${now:%Y%m%d_%H%M%S}

working_dir: ${hydra:runtime.cwd}                      # usually the repo root

  

